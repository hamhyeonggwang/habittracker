# OTD 앱 UI/UX 개선 계획

**분석일:** 2026-06-24  
**대상:** Life Hacking Dashboard (OTD - Own The Day)

---

## 📊 Executive Summary

5개 탭(홈·참여·업무·컨디션·아카이브) 구조에서 **3가지 주요 문제** 확인:

1. **용어 불일치** — 네비게이션과 내부 표현이 다름
2. **데이터 표시 오류** — 에너지맵 점수가 다르게 보임  
3. **정보구조 문제** — 의미있었던 순간의 위치가 부적절

---

## 🔍 상세 분석

### 문제 1: 용어 일관성 부족

#### 현상

**BottomNav.tsx (라인 5-11)**
```jsx
const NAV_ITEMS = [
  { id: 'dashboard', label: '홈', icon: LayoutDashboard },
  { id: 'habit', label: '참여', icon: CheckSquare },      // ← 라벨
  { id: 'task', label: '업무', icon: ListTodo },
  { id: 'mental', label: '컨디션', icon: Activity },
  { id: 'archive', label: '아카이브', icon: BookMarked },
];
```

**HabitPage.tsx에서의 실제 표현 (40+ 곳)**
```
루틴 수정 / 루틴 삭제 / 새 루틴 추가 / 루틴 시간대 / 루틴 이름 등
```

#### 사용자 경험 분석

| 상황 | 사용자 생각 | 실제 | 결과 |
|------|-----------|------|------|
| 앱 시작 후 탭 클릭 | "참여가 뭐지?" | "루틴"을 관리하는 탭 | 혼동 |
| 탭 진입 | "아, 참여하는 것들?" | "일상 루틴" 구조 | 기대 vs 현실 불일치 |
| 다른 사람과 대화 | "습관앱의 '참여' 탭" | 실제로는 "루틴 탭" | 커뮤니케이션 오류 |

#### 영향도
- **높음** — 신규 사용자 온보딩 이탈 가능성
- **누적** — 모든 페이지에서 "루틴"이라 부르므로 혼동 반복

---

### 문제 2: 에너지맵 점수 표시 불일치

#### 현상

**MentalPage.tsx**

레이더 차트 (라인 107-112):
```tsx
<Radar 
  name="오늘" 
  dataKey="value"          // ← 3.2 그대로 표시
  stroke="var(--sage)" 
/>
```

라인 차트 Tooltip (라인 181-184):
```tsx
<Tooltip
  formatter={(v: number, name: string) => [
    getScoreMessage(Math.round(v)) + ` (${v})`,  // ← Math.round + 메시지
    name
  ]}
/>
```

#### 구체적 예

사용자가 에너지 입력: `신체: 3.2, 정서: 3.2, 인지: 3.2, 환경: 3.2`

**레이더 차트에서 보이는 것:**
```
신체 에너지: 3.2
정서 에너지: 3.2
...
```

**라인 차트(일주일 흐름)에서 보이는 것:**
```
Tooltip: "평온한 하루 (3.2)" 
         ↑ Math.round(3.2) = 3 이므로 "평온"
```

#### 왜 문제인가?

1. **시각적 일관성 깨짐** — 같은 점수가 다르게 해석됨
2. **신뢰성 손상** — "점수가 맞나?" 의심
3. **해석 오류** — "어제는 3.2였는데 오늘 차트에선 3이라고?" → 혼동

#### 점수 매핑 테이블 (src/lib/strengthLanguage.ts 참고)
```
1 → "소진했어요"
2 → "힘들어요"
3 → "평온해요"    ← Math.round(3.2) = 3
4 → "활기차요"
5 → "최고예요"
```

**문제**: 3.2는 3과 4 사이의 상태인데, 무조건 3으로 내림

---

### 문제 3: "가장 의미있었던 순간" 배치 문제

#### 현재 구조

**DashboardPage.tsx (라인 735)**

```
┌─────────────────────────────┐
│  Identity 탭                │ ← 메인 섹션
│  - I WILL statements        │
│  - 성장 목표                │
│─────────────────────────────│
│  Roadmap 탭                 │ ← 메인 섹션
│  - 분기별 도전              │
│  - 월별 계획                │
│─────────────────────────────│
│                             │ ← 스크롤 필요
│  💾 오늘 가장 의미...       │ ← 부차적 위치
│  (MeaningfulMomentCard)     │
└─────────────────────────────┘
```

#### 사용자 여정 분석

**시나리오 1: 아침 온보딩**
```
1. "홈" 탭 열기
2. Identity/Roadmap 보기 (탭 전환)
3. 스크롤... 스크롤... 
4. "아, 여기 의미있는 순간!" 발견
5. → 목표와 일관성이 없다고 느낌
```

**시나리오 2: 저녁 성찰**
```
1. "홈" 탭 열기 → 정체성/로드맵 먼저 보임
2. "어? 내 일상 기록은?"
3. Mental 탭으로 이동 → "오늘의 발견" 있음
4. 다시 Dashboard로 → 의미있는 순간 찾기
5. → 성찰 영역이 분산됨 (Dashboard + Mental)
```

#### 정보구조 문제

| 영역 | 현위치 | 의도 | 발견되는 빈도 |
|------|-------|------|-------------|
| Identity/Roadmap | Dashboard 상단 | 방향 제시 | 매일 ★★★★★ |
| 의미있던 순간 | Dashboard 하단 | 성찰 + 회고 | 매일 ★★ |
| 오늘의 발견 | Mental 탭 | 컨디션 성찰 | 일부만 ★★★ |

**현재 배치의 문제점:**
- 의미있는 순간이 보조 정보처럼 보임
- Mental 탭의 "오늘의 발견"과 역할 중복
- 매일의 "성찰" 루틴을 자연스럽게 유도하지 못함

---

## ✅ 개선 방안

### 개선 1: 라벨 통일 (우선순위: 높음)

#### 선택지

**Option A: "참여" → "루틴"**
```jsx
{ id: 'habit', label: '루틴', icon: CheckSquare }
```
- ✅ 내부 표현과 일치
- ✅ 명확한 의미 (일상 루틴)
- ✅ 최소 수정 범위
- ⚠️ "루틴"이 시간대 개념과 혼동될 수 있음 (아침/오후/저녁 루틴)

**Option B: "참여" → "습관"**
```jsx
{ id: 'habit', label: '습관', icon: CheckSquare }
```
- ✅ 일반적 용어
- ✅ 행동 변화의 뉘앙스
- ✅ 진입장벽 낮음
- ⚠️ 내부에서 "루틴"이라 부르므로 일관성 문제 지속

**Option C: "루틴" 전역화 (권장)**
```
BottomNav: 루틴
HabitPage: 루틴 (현상 유지)
내부 API: routine (변경 불필요)
```
- ✅ 완벽한 일관성
- ✅ 전문적 표현
- ✅ 사용자 교육 가능 ("일상 루틴" = 습관)

#### 구현 방법

**파일: src/components/layout/BottomNav.tsx (라인 7)**
```diff
- { id: 'habit', label: '참여', icon: CheckSquare },
+ { id: 'habit', label: '루틴', icon: CheckSquare },
```

**권장:** Option C ("루틴" 전역화) — 1줄 수정

---

### 개선 2: 에너지맵 점수 일관성 (우선순위: 높음)

#### 해결책

**선택지 A: 소수점 유지 + 명확한 메시지**
```tsx
// MentalPage.tsx 라인 183
formatter={(v: number, name: string) => {
  const rounded = Math.round(v);
  const message = getScoreMessage(rounded);
  return [`${message} (${v.toFixed(1)})`, name];
}}
```

**출력:**
```
신체 에너지: 평온해요 (3.2)  ← 정확한 점수 + 의미
정서 에너지: 평온해요 (3.2)
```

**선택지 B: 반올림으로 정렬 (심플)**
```tsx
formatter={(v: number, name: string) => [
  getScoreMessage(Math.round(v)), 
  name
]}
```

**출력:**
```
신체 에너지: 평온해요  ← 심플하지만 정보 손실
```

**선택지 C: 소수점 버림 (데이터 정확성 강조)**
```tsx
formatter={(v: number, name: string) => [
  `${getScoreMessage(Math.floor(v))} (${v.toFixed(1)})`,
  name
]}
```

**권장:** Option A (소수점 유지) — 정확성 + 의미의 균형

#### 구현

**파일: src/components/mental/MentalPage.tsx (라인 181-184)**
```tsx
<Tooltip
  contentStyle={{ fontSize: 12, borderRadius: 8, border: '1px solid var(--border)', fontFamily: 'Pretendard' }}
  formatter={(v: number, name: string) => {
    const message = getScoreMessage(Math.round(v));
    return [`${message} (${v.toFixed(1)})`, name];
  }}
/>
```

---

### 개선 3: "의미있었던 순간" 배치 (우선순위: 중간)

#### 3가지 배치 옵션

**Option A: Dashboard 최상단 이동 (권장)**

```
┌─────────────────────────────┐
│ 📌 오늘 가장 의미...        │ ← 맨 위 (즉시 보임)
│                             │
│ Identity / Roadmap 탭       │ ← 아래로
│                             │
└─────────────────────────────┘
```

**장점:**
- ✅ 첫 화면에서 즉시 노출
- ✅ "하루의 성찰" 루틴 강화
- ✅ 월별 모아보기와 자연스러운 흐름

**단점:**
- ⚠️ Identity/Roadmap의 중요성이 덜해 보일 수 있음

---

**Option B: Mental 탭과 통합 (장기 전략)**

Mental 탭을 "성찰" 탭으로 재구성:

```
컨디션 탭
├─ 에너지맵 (4개 지표)
├─ 에너지 입력
├─ 오늘의 발견 (발견사항)
├─ 의미있었던 순간  ← 통합
└─ 주간 에너지 흐름
```

**장점:**
- ✅ "성찰 영역" 통합
- ✅ 컨디션 + 의미 = 자연스러운 흐름
- ✅ 단일 탭에서 완전한 성찰

**단점:**
- ⚠️ Mental 탭이 너무 무거워짐
- ⚠️ 라벨 변경 필요 (컨디션 → 성찰)

---

**Option C: Archive 탭 "회고" 영역으로 강화 (데이터 활용)**

```
아카이브 탭 (또는 "회고")
├─ 월간 의미있었던 순간들
├─ 월간 컨디션 평균
├─ 완료된 할일/루틴 기록
└─ 분기별 성장 일지
```

**장점:**
- ✅ 과거 데이터 재활용
- ✅ "회고 문화" 강화
- ✅ 장기 추적 가능

**단점:**
- ⚠️ 매일의 입력 동기 약함
- ⚠️ Archive 탭 진입 빈도 낮음

---

#### 권장: Option A + Option B의 혼합

**단기 (즉시, 1주):**
- Option A 적용: Dashboard 최상단으로 이동
- MeaningfulMomentCard를 DashboardPage의 첫 번째 컴포넌트로 변경

**장기 (1개월):**
- Option B 검토: Mental 탭 → "성찰" 탭으로 확장
- Identity/Roadmap ↔ 성찰의 균형 조정

---

## 📋 추가 개선 기회

### 4. 네비게이션 라벨 일관성

**현재:**
```
홈 | 참여 | 업무 | 컨디션 | 아카이브
```

**개선 후:**
```
홈 | 루틴 | 할일 | 컨디션 | 기록
```

또는 (OT 중심):
```
홈 | 역할 | 할일 | 에너지 | 회고
```

| 라벨 | 장점 | 단점 |
|------|------|------|
| 루틴 | 내부 표현과 일치 | 시간대 개념 혼동 가능 |
| 할일 | 간단명료 | "업무"와 차이 불명확 |
| 기록 | 아카이브의 본질 | 수동적 느낌 |
| 회고 | 목적 명확 | 용어가 다소 무거움 |

---

## 🚀 실행 계획

### Phase 1: 긴급 개선 (1-2시간)

1. **라벨 통일**
   - [ ] BottomNav: "참여" → "루틴"
   - [ ] QA: 모든 화면에서 "루틴" 표시 확인

2. **에너지맵 포맷터 수정**
   - [ ] MentalPage Tooltip 수정
   - [ ] 테스트: 3.2 입력 → 라인 차트에서 "평온해요 (3.2)" 표시 확인

### Phase 2: UI 배치 개선 (2-4시간)

3. **의미있었던 순간 배치**
   - [ ] DashboardPage 컴포넌트 순서 변경
   - [ ] 모바일 뷰포트에서 레이아웃 확인
   - [ ] 스크롤 깊이 최소화 확인

4. **온보딩 업데이트**
   - [ ] "루틴" 설명 업데이트
   - [ ] 컨디션 기록 가이드 추가

### Phase 3: 검증 (1시간)

5. **사용자 테스트**
   - [ ] 5명 신규 사용자 피드백
   - [ ] "루틴이 뭐예요?" vs "루틴이네요" 반응 비교
   - [ ] 의미있었던 순간 기록 빈도 추적

---

## 📝 변경 체크리스트

**즉시 적용 (Phase 1):**
- [ ] `src/components/layout/BottomNav.tsx` 라인 7 수정
- [ ] `src/components/mental/MentalPage.tsx` 라인 181-184 수정
- [ ] 테스트 및 빌드 확인

**다음 반복 (Phase 2):**
- [ ] `src/components/dashboard/DashboardPage.tsx` 라인 735 앞으로 이동
- [ ] 스크롤 깊이/정보구조 재검토
- [ ] 온보딩 가이드 업데이트

---

## 참고: 코드 위치 요약

| 문제 | 파일 | 라인 | 액션 |
|-----|------|------|------|
| 라벨 | BottomNav.tsx | 7 | "참여" → "루틴" |
| 에너지맵 | MentalPage.tsx | 181-184 | Tooltip 포맷터 수정 |
| 배치 | DashboardPage.tsx | 735 | MeaningfulMomentCard 이동 |

