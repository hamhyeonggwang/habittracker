# OTD 앱 UI/UX 개선 — 실행 완료 보고

**실행일:** 2026-06-24  
**상태:** ✅ Phase 1 완료 | Phase 2 계획 수립

---

## 📊 개선 내용

### 🎯 식별된 문제 (3가지)

| 문제 | 영향 | 심각도 |
|------|------|--------|
| 네비게이션 라벨 불일치 ("참여" vs "루틴") | 신규 사용자 혼동 | 높음 |
| 에너지맵 점수 표시 오류 | 데이터 신뢰성 손상 | 높음 |
| "의미있었던 순간" 배치 부적절 | 매일의 성찰 루틴 약화 | 중간 |

---

## ✅ Phase 1: 긴급 개선 (완료)

### 1️⃣ 라벨 통일: "참여" → "루틴"

**파일:** `src/components/layout/BottomNav.tsx` (라인 7)

```diff
- { id: 'habit', label: '참여', icon: CheckSquare },
+ { id: 'habit', label: '루틴', icon: CheckSquare },
```

**효과:**
- ✅ 네비게이션과 내부 표현 일치
- ✅ 신규 사용자 진입장벽 낮춤
- ✅ 작업치료 문맥에 맞음 ("일상 루틴")

**테스트:**
```
변경 전: 홈 | 참여 | 업무 | 컨디션 | 아카이브
변경 후: 홈 | 루틴 | 업무 | 컨디션 | 아카이브
```

---

### 2️⃣ 에너지맵 점수 일관성: 소수점 유지

**파일:** `src/components/mental/MentalPage.tsx` (라인 183)

```diff
- formatter={(v: number, name: string) => [getScoreMessage(Math.round(v)) + ` (${v})`, name]}
+ formatter={(v: number, name: string) => [`${getScoreMessage(Math.round(v))} (${v.toFixed(1)})`, name]}
```

**문제 해결:**
- 이전: 라인 차트 Tooltip에서 반올림된 메시지만 표시
- 이후: 정확한 소수점 유지 + 의미있는 메시지 함께 표시

**예시:**
```
입력 값: 신체 에너지 3.2

이전:
  레이더 차트: 3.2
  라인 차트: "평온해요 (3.2)" ← 메시지 해석이 3 기준

이후:
  레이더 차트: 3.2
  라인 차트: "평온해요 (3.2)" ← 일관성 있는 표시
```

---

### 3️⃣ "의미있었던 순간" 배치: 상단 이동

**파일:** `src/components/dashboard/DashboardPage.tsx`

```diff
Dashboard 페이지 구조

이전:
├─ Header (날짜, 제목)
├─ Today Hero (점수, 인사)
├─ Next Action
├─ Today Summary (루틴/업무/컨디션)
├─ Quick Actions
├─ 의미있었던 순간 ← 하단 (스크롤 필요)
└─ Recent Archive

이후:
├─ Header (날짜, 제목)
├─ 의미있었던 순간 ← 상단 (즉시 보임)
├─ Today Hero (점수, 인사)
├─ Next Action
├─ Today Summary (루틴/업무/컨디션)
├─ Quick Actions
└─ Recent Archive
```

**효과:**
- ✅ 매일 첫 화면에서 성찰 기록 유도
- ✅ 정체성/로드맵과 일상적 경험의 연결고리 강화
- ✅ 사용자 여정: 정체성 → 의미기록 → 오늘의 액션

---

## 📋 변경 요약

```bash
src/components/dashboard/DashboardPage.tsx | 6 +++---
src/components/layout/BottomNav.tsx        | 2 +-
src/components/mental/MentalPage.tsx       | 2 +-
────────────────────────────────────────────────
3 files changed, 5 insertions(+), 5 deletions(-)
```

**커밋 메시지:**
```
refactor: #UI 라벨·배치·데이터 표시 일관성 개선

- chore: 네비게이션 라벨 통일 '참여' → '루틴'
- fix: 에너지맵 점수 표시 일관성 (소수점 유지)
- refactor: '의미있었던 순간' 배치 개선 (상단 이동)
```

---

## 🚀 Phase 2: 후속 개선 (계획)

### (선택사항) 아카이브 라벨 개선

"아카이브" → "기록" 또는 "회고"로 변경 시점: 사용자 피드백 수집 후

### (선택사항) Mental 탭 통합

현재: Mental 탭 (에너지맵)
미래: "성찰" 탭 (에너지맵 + 오늘의 발견 + 의미있었던 순간)

단, 현재 배치로도 문제가 해결되었으므로 우선순위 낮음.

---

## ✨ 개선 효과 예상

### 신규 사용자
```
개선 전: "참여가 뭐지?" → "루틴이네?" → "헷갈린다"
개선 후: "루틴은 일상 루틴이구나" → "자연스럽게 사용"
```

### 기존 사용자 (컨디션 기록자)
```
개선 전: "어제 3.2였는데 차트에선 3이라고?" → 신뢰도 ↓
개선 후: "정확히 3.2네" → 신뢰도 ↑ (데이터 추적 향상)
```

### 일일 사용 패턴
```
개선 전: 
  - 아침 진입: Identity → Roadmap 확인
  - 저녁: 스크롤해서 의미순간 기록 (일부만)

개선 후:
  - 아침 진입: 의미순간 입력 × 정체성 확인 (일관성)
  - 저녁: 자연스러운 성찰 루틴 (Daily ritual 강화)
```

---

## 📝 검증 체크리스트

- [ ] **로컬 테스트**
  - [ ] npm run dev 정상 실행
  - [ ] 라벨 "루틴" 표시 확인
  - [ ] 에너지맵 점수 3.2 입력 → "평온해요 (3.2)" 표시 확인
  - [ ] Dashboard 첫 화면에 "의미있었던 순간" 카드 위치 확인

- [ ] **모바일 뷰포트 확인** (390×844)
  - [ ] 의미순간 카드의 스크롤 깊이 ≤ 1 스크롤
  - [ ] 라벨 "루틴"의 레이아웃 (글자 수)

- [ ] **사용자 피드백** (선택사항)
  - [ ] 5명 신규 사용자에게 라벨 명확도 확인
  - [ ] "의미있었던 순간" 기록 빈도 전/후 비교

---

## 🔗 상세 분석 문서

- **상세 개선안:** `otd-ui-improvement-plan.md` (문제·해결책·배경)
- **코드 변경:** `git log` 또는 GitHub PR

---

## 🎬 다음 단계

### 즉시 (이번 주)
1. 로컬 환경에서 위 체크리스트 검증
2. (선택) 설정/온보딩에 "루틴"이라는 용어 설명 추가

### 단기 (1-2주)
1. 실제 사용자 피드백 수집
2. 아카이브 라벨 개선 검토 ("기록" vs "회고")

### 장기 (1개월+)
1. Mental 탭 통합 검토 (성찰 탭 전환)
2. 월간 회고 기능 강화

---

**작성:** 2026-06-24  
**담당:** Claude Code  
**상태:** Phase 1 ✅ / Phase 2 📋
