# OTD 최종 기획회의 — PM / UX / UI / QA 심층 검토

**대상**: OTD (Own The Day) — 공개 출시 v1
**일자**: 2026-06-20
**전제**: 불특정 다수 정식 공개 / 무료 / AI 제외 / 멀티테넌트 / Google+Kakao 로그인
**용도**: `otdh-launch-spec.md`와 함께 Claude Code 작업 컨텍스트로 사용

> 이 문서는 "무엇을 만들지(spec)"가 아니라 **"지금 설계의 약점이 무엇인지"**를 네 관점에서 비판적으로 검토한 회의록이다.

---

## ★ 포지셔닝 확정 (2026-06-20) — 이후 모든 결정의 기준

**OTD는 B2C 대중 습관앱이 아니라, "치료사가 클라이언트에게 직접 설치를 권유하는 임상 보조 도구"다.**

함의:
- **전문적 정체성 유지** (U1 결정 = B/C 방향). 전문 용어는 약점이 아니라 신뢰 자산. 단 클라이언트 단독 사용 대비 툴팁·도움말 보강.
- 사용 맥락이 **"치료사 동반 설치"** + **"클라이언트 단독 재사용"** 두 갈래 → 온보딩/튜토리얼도 두 경로 설계.
- 대상에 **아동·지체장애 당사자 포함**(선생님 병원 특성) → **접근성이 선택이 아니라 임상 요건**. 아래 U3를 P1으로 승격.

---

## 🔴 P0 — 출시 전 반드시 해결 (Blocker)

### [QA] B1. `TODAY` 자정 고정 버그 (확정: P0, 수정 필수)
- `src/lib/utils.ts`의 `export const TODAY = format(new Date(), 'yyyy-MM-dd')`가 **모듈 로드 시점에 1회 고정**됨.
- 사용자가 앱을 열어둔 채 자정을 넘기면, 이후 모든 기록(습관 체크·컨디션·업무)이 **전날 날짜로 저장**됨.
- 습관 트래커에서 "오늘"의 정확성은 제품의 근간 → 치명적.
- **해결**: `TODAY` 상수를 함수 `getToday()`로 전환, 기록/저장 시점마다 호출. 또는 앱 포커스 복귀(visibilitychange) 시 날짜 갱신.

### [PM/UX] B2. 온보딩 + 튜토리얼 부재 (확정: P0)
- 가입 직후 **빈 대시보드**로 직행 (스플래시 0.9초 → 바로 빈 화면). 역할 0개, 습관 0개, 안내 0개.
- 포지셔닝(치료사 권유 설치)에 맞춰 **두 경로** 설계:
  - **치료사 동반 설치**: 역할·첫 습관을 옆에서 같이 설정하는 빠른 셋업. "건너뛰기" 허용.
  - **클라이언트 단독 재사용**: 나중에 혼자 열었을 때를 위한 **상시 튜토리얼** — 각 탭 첫 진입 시 코치마크(coach mark), 화면 내 도움말(?) 버튼으로 언제든 재호출 가능.
- 온보딩 1회성 제어: `profiles.onboarding_completed`. 튜토리얼은 1회 자동 노출 + 수동 재호출.
- spec P1-6(역할 커스터마이즈)과 통합 설계. 역할 제안 풀 + 첫 습관 만들기 + 핵심 탭 1줄 설명 포함.

### [QA] B1. `TODAY` 자정 고정 버그 (확정: P0, 수정 필수)

### [QA] B3. 저장 실패 무피드백 (silent failure)
- 전 페이지의 `handleSave`가 `await store.save()` 후 성공/실패 구분 없이 무조건 "저장됨" 표시 (`MentalPage` 등).
- `storage.ts`는 실패 시 `console.error`만 남기고 throw하지 않음 → 사용자는 저장 안 됐는데 됐다고 착각.
- 공개 환경(불안정 네트워크·RLS 거부)에서 데이터 유실로 직결.
- **해결**: storage 함수가 `{ ok, error }` 반환하도록 리팩토링 → 실패 시 toast/배너로 명시. (spec QA 5의 "에러 피드백 부재"와 동일 건, P0로 승격)

---

## 🟠 P1 — 출시 품질에 중요 (공개 타겟 정합성)

### [UX] U1. 직무 용어 — ✅ 결정: 전문성 유지 (C 절충)
- 포지셔닝(치료사 권유)에 따라 **전문 용어 유지 = 신뢰 자산**. 대중어 전면 번역(A)은 폐기.
- 단, 클라이언트 단독 사용 대비:
  - 컨디션 4축 영문 임상 라벨(`Physical Capacity` 등)은 **한글 라벨 우선 + 영문 부가** 순서로 조정.
  - 전문 용어(참여/Occupation, 임상 카테고리 등)에 **툴팁·도움말 1줄 풀이** 부착 (B2 튜토리얼과 연계).
  - "임상" 아카이브 카테고리는 유지하되, 의료직이 아닌 클라이언트에겐 혼란 가능 → 카테고리 라벨 재검토(예: "치료/재활 기록")는 백로그.

### [PM] U2. 제품 정체성 = "해빗트래커"인가 "라이프 OS"인가
- 현재 5개 탭 + 대시보드의 정체성선언/목표/분기로드맵/재무까지 → 기능 범위가 매우 넓음("라이프 OS").
- 스토어에서 "습관 앱"으로 검색해 들어온 사용자에겐 과함(인지 부하), "올인원"을 원하는 사용자에겐 매력.
- v1 전 범위 포함은 이미 확정됨. 다만 **스토어 포지셔닝 문구**는 둘 중 하나로 정렬해야 검색·전환이 산다.
- **결정 필요**: 스토어 1줄 소개를 "습관" 중심으로 갈지 "하루 통합 관리" 중심으로 갈지.

### [UI] U3. 접근성 — ✅ P1 승격 (임상 요건)
- 대상에 아동·지체장애 당사자 포함 → 접근성이 선택이 아니라 **임상 신뢰도 직결 요건**.
- **대비비**: `--text-muted: #8a9a82` on `#fff`는 WCAG AA 4.5:1 미달 의심. 특히 11px 소형 텍스트 다수 → 대비·크기 동반 점검 필수.
- **확대 차단 제거**: viewport `maximum-scale=1`(현 layout) → 확대 차단은 접근성 위반. 제거 (spec P3 포함, 확인).
- **터치 타깃**: 하단 내비·점수 선택·체크 버튼이 최소 44×44pt 확보되는지 점검.
- **글자 크기**: 시스템 글꼴 확대 설정 존중(고정 px 남용 지점 점검).
- 다크모드: 야간 사용 대비 권장이나, 위 항목보다 우선순위 낮음 → 백로그 가능.

### [UX] U4. 역할 고정 → 사용자화 (spec P1-6과 연결)
- 이미 spec 반영됨. UX 관점 보강: 역할 삭제 시 해당 역할로 필터링된 통계/대시보드가 깨지지 않는지(빈 상태 처리), 역할 0개일 때 습관 등록 플로우가 막히지 않는지(역할은 선택사항이어야) 확인.

---

## 🟡 P2 — 개선 권장 (출시 후 가능)

| 관점 | 항목 | 내용 |
|------|------|------|
| QA | 삭제 confirm 일관성 | 습관·아카이브는 `window.confirm` 사용. 네이티브 confirm은 TWA에서 투박함 + 페이지마다 패턴 불일치. 공용 확인 모달로 통일 권장 |
| UX | 빈 상태(empty state) 품질 | `EmptyState` 컴포넌트는 있으나 페이지별 적용 편차. 모든 리스트의 0건 상태에 "다음 행동" CTA 보장 |
| UI | 로딩 스켈레톤 부재 | fetch 중 화면이 "빈 상태"와 구분 안 됨 → 깜빡임. skeleton 추가 |
| UX | 컨디션 저장 토스트가 2초 자동소멸 | 저장 성공만 알리고 실패는 안 알림(B3과 연결). 또한 입력 변경 후 미저장 이탈 시 경고 없음 |
| QA | recharts 번들 무게 | 차트 다수 사용. 초기 로딩·TWA 체감속도에 영향. 동적 import 검토 |
| UI | 색상 의미 과다 | sage(녹)·navy(남)·역할별 6색·카테고리 6색·컨디션 4색 → 화면당 색이 많아 위계가 흐려짐. 색 역할 정리 권장 |
| PM | 데이터 내보내기 부재 | 민감 데이터(컨디션·재무)를 다루는데 사용자 본인 데이터 export 기능 없음. 신뢰·이탈 대비 + (P4 계정삭제와 함께) 데이터 이동권 측면 |

---

## 관점별 한 줄 총평

- **PM**: 범위가 넓은 건 무기이자 약점. v1은 "기능 추가"가 아니라 **온보딩·포지셔닝·신뢰(데이터 안전)** 마감에 자원을 써야 함.
- **UX**: 가장 큰 리스크는 버그가 아니라 **"전문가의 도구를 대중 언어로 포장만 한 불일치"**. 용어 정렬(U1)이 핵심 결정.
- **UI**: 완성도는 이미 높음(타이포·카드·차트). 색 위계 정리와 접근성(대비·확대·다크모드)만 보강하면 스토어 경쟁력 충분.
- **QA**: 코드 구조는 양호하나, **날짜 고정(B1)·저장 실패 은폐(B3)** 두 가지가 데이터 신뢰를 흔드는 급소. 멀티테넌트 전환과 함께 최우선.

---

## 이 회의에서 내려야 할 결정 (체크리스트)

- [x] **포지셔닝**: 치료사 권유형 임상 보조 도구로 확정 (B2C 대중앱 아님)
- [x] **U1**: 직무 용어 → 전문성 유지(C 절충: 한글 우선+영문 부가, 툴팁 풀이)
- [x] **B1·B2·B3**: P0 확정 → spec 본문 작업 항목에 편입 필요
- [x] **B2 온보딩**: 치료사 동반 + 클라이언트 단독 두 경로 + 상시 튜토리얼(코치마크/도움말)
- [x] **U3 접근성**: P1 승격(임상 요건). 다크모드만 백로그.
- [x] **U2**: 스토어 소개 → "작업치료사가 설계한 자기관리 도구" 신뢰 소구로 확정. 단 Play 의료·건강 심사 대비 효능/치료 약속 표현 금지(치료·재활효과·증상개선·의학적 등 배제)
- [ ] **P2 데이터 내보내기**: v1 포함 vs 백로그
- [ ] 패키지명 `kr.othub.otd` 최종 확정 (기존 미결)

---

## 다음 액션: spec 문서 반영 필요 항목
아래는 `otdh-launch-spec.md`에 작업 항목으로 편입해야 할 확정 사항:
1. **P0-A**: `TODAY` → `getToday()` 전환 (자정 버그)
2. **P0-B**: 온보딩(2경로) + 상시 튜토리얼 — P1-6 역할 설정과 통합
3. **P0-C**: storage 함수 `{ok,error}` 반환 + 저장 실패 toast
4. **P1 접근성**: 대비비·확대허용·터치타깃·글자크기 (임상 요건)
5. **P1 용어**: 컨디션 4축 한글 우선화 + 전문용어 툴팁
